version: '3.8'

services:
  certbot:
    container_name: cmsapp-web-certbot
    build: ./services/certbot
    command: certonly --webroot --webroot-path /var/www/certbot/ --dry-run -d jumpifzer0.me --email test@test.com
    image: certbot/certbot:latest
    depends_on:
      - nginx
    volumes:
      - ./certbot/www/:/var/www/certbot/:rw
      - ./certbot/conf/:/etc/letsencrypt/:rw
  nginx:
    container_name: cmsapp-proxy
    build: ./services/nginx
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
    networks:
      - frontend
    volumes:
      - ./certbot/www:/var/www/certbot/:ro
      - ./certbot/conf/:/etc/nginx/ssl/:ro
  web:
    container_name: cmsapp-web
    build: ./services/web
    command: gunicorn --bind 0.0.0.0:32984 manage:app
    expose:
      - 32984
    env_file:
      - ./.env.dev
    depends_on:
      - db
    networks:
      - frontend
      - backend
  db:
    container_name: cmsapp-db
    image: postgres:13-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.db.dev
    networks:
      - backend


networks:
  frontend:
  backend:

volumes:
  postgres_data: